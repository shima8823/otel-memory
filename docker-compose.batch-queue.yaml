# Batch x Queue シナリオ用 Docker Compose オーバーライド
#
# Jaeger の CPU を制限して「バックエンドが遅いが完全停止はしない」状態を模擬する。

services:
  jaeger:
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 2G
